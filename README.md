# Installation
- `composer install`
- `copy env`
- `php artisan key:generate`
- `npm install`
- `npm install vue-router`

# Usage
> Build and Watch
- `php artisan serve`
- `npm start` or `npm run watch`
